/* 
Obtiene el logo de la empresa, el cual se muestra en la esquina superior
*/

const Logo = (props) => (
    <img
      alt="Logo"
      src="/static/logo.png"
      {...props}
    />
  );  
  
  export default Logo;
  