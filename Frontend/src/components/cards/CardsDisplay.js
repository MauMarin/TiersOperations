import React from 'react'

function CardsDisplay({cards, ...rest}) {
    cards.map((card) => {
        console.log(card)
    })
    return (
        <div>
            
        </div>
    )
}

export default CardsDisplay;
